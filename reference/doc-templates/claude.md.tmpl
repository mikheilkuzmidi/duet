# {{PROJECT_NAME}}

{{ONE_SENTENCE_WHAT_THIS_IS}}

**Read `AGENTS.md` before doing anything.** It holds the rules that apply to
every agent working here, and this file only points at it so that the two can
never disagree.

## The map

| File | What it holds |
|---|---|
| `AGENTS.md` | How to work in this repo. Read first. |
| `docs/architecture.md` | How it is built and why. The shape you must not drift from. |
| `docs/product.md` | What it does, who for, and what is deliberately out of scope. |
| `docs/decisions.md` | Every choice that has been made, with its reasoning. |
| `docs/style.md` | How code and words look here. |
| `docs/testing.md` | What gets tested and how to run it. |
| `docs/done.md` | What finished means. Check against this before saying done. |

## Run it

```bash
{{INSTALL_COMMAND}}
{{DEV_COMMAND}}
{{TEST_COMMAND}}
```

## Before you change anything

1. Read `docs/architecture.md`. Match it. If your change requires departing from
   it, that is a decision, and it belongs in `docs/decisions.md` before the code.
2. Read `docs/style.md`. Match the code around you, not your own defaults.
3. Check `docs/done.md` before reporting finished.
